# Core deep learning stack, pinned to exact versions verified working together
# (CUDA 12.4 build of torch, tested on an RTX 3050 6GB laptop GPU).
#
# Install torch first with the CUDA index, then the rest:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
torch==2.6.0
transformers==5.14.1
peft==0.19.1
datasets==5.0.0
accelerate==1.14.0
evaluate==0.4.6
sentencepiece==0.2.2
protobuf==7.35.1
pillow==12.3.0
jiwer==4.0.0
matplotlib==3.11.1
